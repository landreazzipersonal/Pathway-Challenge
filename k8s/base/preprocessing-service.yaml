apiVersion: v1
kind: Service
metadata:
  name: preprocessing-service
  namespace: devops-challenge
spec:
  selector:
    app: preprocessing-service
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
  type: ClusterIP